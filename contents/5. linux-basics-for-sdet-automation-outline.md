# Linux Basics for SDET Automation Course Outline

## Linux Fundamentals for SDETs
- Why Linux knowledge matters in test automation and CI/CD
- Linux distributions and common environments
- The terminal and shell
- Bash overview
- Command syntax, options, arguments, and exit codes
- Getting help with `man`, `--help`, and `help`
- Safe command-line habits

## Filesystem Navigation
- Linux filesystem hierarchy
- Absolute and relative paths
- Current directory with `pwd`
- Listing files with `ls`
- Changing directories with `cd`
- Creating directories with `mkdir`
- Creating files with `touch`
- Copying, moving, renaming, and removing files
- File glob patterns
- Paths with spaces and shell quoting

## Viewing and Editing Files
- Viewing small and large files with `cat`, `less`, `head`, and `tail`
- Following live logs with `tail -f`
- Searching text with `grep` and `rg`
- Finding files with `find` and `rg --files`
- Counting lines and words with `wc`
- Sorting and removing duplicate lines
- Basic text processing with `cut`, `sort`, `uniq`, `tr`, and `xargs`
- Editing files with `nano`, `vim`, or another terminal editor

## Permissions and Ownership
- Users, groups, and ownership
- Read, write, and execute permissions
- Understanding permission notation
- Changing permissions with `chmod`
- Changing ownership with `chown`
- Executable shell scripts
- `sudo` and least privilege
- File permissions for test artifacts, mounted volumes, and CI workspaces

## Shell Variables and Environment Configuration
- Shell variables and environment variables
- Exporting variables
- Reading environment variables with `printenv` and `env`
- `PATH` and executable discovery with `which` and `command -v`
- Configuration files such as `.bashrc` and `.zshrc`
- Setting test environment URLs, browser settings, and feature flags
- Avoiding secrets in shell history and scripts
- Using `.env` files safely

## Processes and Job Control
- Processes, process IDs, and parent processes
- Listing processes with `ps`, `top`, and `htop`
- Finding processes with `pgrep`
- Starting background processes with `&`
- Foreground and background jobs
- Stopping processes with `kill` and signals
- Graceful shutdown versus forceful termination
- Diagnosing hung test runners, browsers, and local servers

## Networking and Connectivity
- Hostnames, IP addresses, ports, and DNS
- Checking connectivity with `ping`
- HTTP requests with `curl`
- Downloading files with `curl` and `wget`
- Inspecting open ports with `lsof`, `ss`, or `netstat`
- Checking DNS resolution
- Testing local services and health endpoints
- Diagnosing proxy, TLS, and connection-refused failures

## Archives and File Transfer
- Creating and extracting tar archives
- Working with gzip and zip files
- Compressing test artifacts and logs
- Secure file copying with `scp`
- Remote file transfer with `rsync`
- Transferring reports from remote environments

## Bash Scripting for Automation
- Shebang and script execution
- Variables and quoting rules
- Command substitution
- Conditionals: `if`, `case`, and test expressions
- Loops: `for`, `while`, and `until`
- Functions
- Positional parameters and command-line arguments
- Exit codes and error handling
- Strict mode: `set -euo pipefail`
- Logging and debugging scripts with `set -x`

## Test Automation Utilities
- Starting and stopping a local application under test
- Polling health endpoints before test execution
- Running test commands and preserving exit status
- Passing environment configuration to test runners
- Creating unique test-data identifiers
- Collecting logs after a failure
- Archiving screenshots, videos, traces, and reports
- Cleaning temporary test resources safely

## Package Management and Runtime Setup
- Package managers overview
- Installing command-line dependencies
- Updating package indexes and packages
- Node.js, Java, Python, and browser dependencies
- Checking tool versions
- Reproducible environment setup scripts
- Managing dependencies in CI runners and containers

## Logs and Troubleshooting
- Standard output and standard error
- Redirecting output with `>` and `>>`
- Piping commands with `|`
- Capturing command output
- Searching logs for errors and warnings
- Reading system and service logs
- Diagnosing permission, disk-space, memory, and port-conflict issues
- Reproducing CI failures locally

## Remote Linux Environments
- Connecting with SSH
- SSH keys and known hosts
- Running remote commands
- SSH configuration files
- Tunneling and port forwarding basics
- Working safely on shared test environments
- Session tools such as `tmux` and `screen`

## Linux in Docker and CI
- Linux containers and filesystem behavior
- Bind mounts and volume permissions
- Container user IDs and file ownership
- Shell commands in Dockerfiles and entrypoints
- Debugging containers with shell access and logs
- Linux runners in GitHub Actions
- Writing portable CI shell scripts

## Hands-On Projects
- Navigate, search, and process test-log files from the terminal
- Write a Bash script that starts an application and waits for health checks
- Run an automated test suite and archive its results on failure
- Diagnose a deliberately broken service using logs and network tools
- Create a reusable environment-setup script for an automation project
- Connect to a remote test environment and collect diagnostic artifacts
- Run a Dockerized test command and resolve a mounted-volume permission issue
